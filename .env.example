# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Magic-link redirect base (production: https://your-domain.com)
NEXT_PUBLIC_SITE_URL=

# Optional: Postgres password for migration drift script / local SQL apply
SUPABASE_DB_PASSWORD=

# Dev-only (NODE_ENV !== production): bypass auth locally — ignored in production builds
DEV_NO_AUTH=
DEV_USER_ID=

# Anthropic (co-pilot + episode summaries)
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=

# Image / video providers
OPENAI_API_KEY=
OPENAI_IMAGE_MODEL=
OPENAI_IMAGE_MODERATION=
FAL_KEY=
SEEDANCE_MODEL=
SEEDANCE_FAST_MODEL=
XAI_API_KEY=

# Azure Speech (voice — stubbed until provider wired)
AZURE_SPEECH_KEY=
AZURE_SPEECH_REGION=

# CLI-only (reconcile stuck takes)
RECONCILE_REQUEST_MAP=

# Test scripts only
TEST_CREDITS_USER_ID=
